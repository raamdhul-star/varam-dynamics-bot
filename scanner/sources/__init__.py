# scanner.sources — per-platform data-source adapters (Sprint D2+).
# Each adapter normalizes a venue's market/candle data into the common
# internal format the scorer already expects. Hyperliquid keeps using the
# existing scanner/fetcher.py + scanner/assets.py untouched.
